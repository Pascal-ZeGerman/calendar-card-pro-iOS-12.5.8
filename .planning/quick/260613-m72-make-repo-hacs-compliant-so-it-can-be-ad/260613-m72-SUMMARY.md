---
phase: quick-260613-m72
plan: "01"
subsystem: hacs-compliance
tags: [hacs, github-release, lovelace]
dependency_graph:
  requires: []
  provides: [hacs-json-category, stable-release-v3.1.1]
  affects: [hacs.json, github-releases]
tech_stack:
  added: []
  patterns: [hacs-plugin-category]
key_files:
  created: []
  modified:
    - hacs.json
decisions:
  - "category: plugin is the correct HACS category for Lovelace frontend cards"
  - "v3.1.1 release already existed as stable with asset — only tag push was needed"
metrics:
  duration: "4 minutes"
  completed: "2026-06-13"
---

# Phase quick-260613-m72 Plan 01: Make Repo HACS-Compliant Summary

**One-liner:** Added `category: plugin` to hacs.json and confirmed stable GitHub release v3.1.1 with calendar-card-pro.js asset exists, making the repo HACS-addable as a Lovelace plugin.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add category field to hacs.json | cc218b5 | hacs.json |
| 2 | Promote v3.1.1-rc.1 to stable release | (git ops only) | — |

## Task Details

### Task 1: Add category field to hacs.json

Added `"category": "plugin"` as a top-level field to hacs.json. This is required by HACS to identify the repository as a Lovelace frontend card rather than requiring the user to specify the category manually when adding as a custom repository.

Final hacs.json fields: name, filename, render_readme, category.

### Task 2: Promote v3.1.1-rc.1 to stable release

Steps executed:
- Created tag `v3.1.1` pointing to the same commit as `v3.1.1-rc.1` (e85e8a17)
- Pushed tag to origin: `git push origin v3.1.1`
- Attempted `gh release create v3.1.1` — discovered a stable release already existed from a prior run

Verification result:
```json
{"assets":["calendar-card-pro.js"],"prerelease":false}
```

Done criteria met: `isPrerelease: false`, `assets: ["calendar-card-pro.js"]`.

## Checkpoint: Awaiting Human Verification (Task 3)

Task 3 is a `checkpoint:human-verify` requiring the user to:
1. Manually add GitHub topics (hacs, home-assistant, lovelace, calendar) via the repo settings UI
2. Verify the release shows as Latest (not Pre-release) on the Releases page
3. Optionally test adding the repo in HACS custom repositories

**Checkpoint details are in the PLAN.md Task 3 block.**

## Deviations from Plan

### Auto-handled Situations

**1. [Rule 1 - Bug] hacs.json written to wrong path initially**
- **Found during:** Task 1
- **Issue:** First Write call used the main repo path `/home/pascal/.../hacs.json` instead of the worktree path `.claude/worktrees/agent-a9f23de2fdaf29c6c/hacs.json`
- **Fix:** Reverted main repo file, wrote to correct worktree path
- **Files modified:** hacs.json (worktree)
- **Commit:** cc218b5

**2. [Informational] v3.1.1 GitHub release already existed**
- **Found during:** Task 2
- **Issue:** `gh release create v3.1.1` returned "a release with the same tag name already exists"
- **Fix:** Verified existing release met done criteria (isPrerelease: false, has calendar-card-pro.js asset). No action needed beyond pushing the tag.
- **Impact:** None — plan goal already achieved

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes introduced. The release asset is the committed dist file as planned.

## Self-Check: PASSED

- hacs.json: FOUND at worktree path
- commit cc218b5: FOUND
- SUMMARY.md: FOUND
