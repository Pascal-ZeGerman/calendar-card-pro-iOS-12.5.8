---
context: default
phase: null
task: 2
total_tasks: 6
status: in_progress
last_updated: 2026-06-10T18:58:22Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Current State

<current_state>
Executing PR #1 bug fixes using superpowers:subagent-driven-development.
Branch: `gsd/phase-01-build-pipeline`
Task 1 DONE at commit `81f12af`. Ready to start Task 2.

Spec reviewer flagged one ADVISORY gap on Task 1: `tests/test-nan-guards.js` stubs
omit the `console.warn` call — production code at `src-lite/calendar-card-pro.js:274,286`
is correct. Accepted and moving on.
</current_state>

## Completed Work

<completed_work>
- Task 1: NaN date guards in `eventStart` and `eventEnd` — DONE (commit `81f12af`)
  - `eventStart` and `eventEnd` now return `null` (with `console.warn`) for unparseable datetimes
  - `tests/test-nan-guards.js` created, 6 assertions, all pass
  - Advisory: test stubs don't verify `console.warn` side-effect (production code is correct)
</completed_work>

## Remaining Work

<remaining_work>
- Task 2: Extract `renderEventTimeText` helper; fix `renderCard` (C2, C4) — **START HERE**
- Task 3: Add `_renderMessage`; fix fetch-failure error surface (C1, I1)
- Task 4: Loading placeholder, expand-state persistence, stale-hass re-render (I2, I3, I4, I6)
- Task 5: Validate entity entries; throw on zero valid entities (I5)
- Task 6: Comment fixes (verify-bundle.sh, helpers.ts, calendar-card-pro.js)
</remaining_work>

## Decisions Made

<decisions_made>
- Scope: all 10 findings (C1-C4 + I1-I6 + comment fixes) — user chose full scope
- Approach B: extract `renderEventTimeText` + `_renderMessage` helpers (cleaner, testable)
- Task 1 spec reviewer advisory gap accepted — test stubs don't call console.warn, but production code does
</decisions_made>

## Required Reading (in order)

1. `docs/superpowers/plans/2026-06-09-pr1-bug-fixes.md` — full task text for Tasks 2-6
2. `docs/superpowers/specs/2026-06-09-pr1-bug-fixes-design.md` — architecture and per-bug design decisions
3. `src-lite/calendar-card-pro.js` on branch `gsd/phase-01-build-pipeline` — the file being fixed (now ~848 lines after Task 1 additions)

## Infrastructure State

- Branch: `gsd/phase-01-build-pipeline` (NOT `fix/ios12-no-events-visible` which is the current branch)
- No running servers
- No build step needed for Tasks 2-6 (source-level edits only; build only needed for post-implementation verify-bundle.sh check)
- Node.js test runner: `node tests/test-*.js` (no framework, built-in `assert` only)

## Execution Method

Using `superpowers:subagent-driven-development`:
- Fresh subagent per task (model: sonnet for Tasks 2-5, haiku for Task 6 comments)
- Spec compliance review after each implementer
- Code quality review after spec passes
- Plan path: `docs/superpowers/plans/2026-06-09-pr1-bug-fixes.md`

<context>
This session: code-reviewed PR #1 (10 findings), brainstormed fix approach (chose B), wrote spec + plan, executed Task 1. Context exhausted at 87% before Task 2 could start.

The plan is fully written with exact code for every step — the implementer subagents need the plan task text plus context about the branch and file, nothing else.

Task 2 is the most architecturally interesting: adding the `renderEventTimeText` pure function (after `monthShort`, before the section 4 banner) and replacing ~12 lines of time-render logic in `renderCard` with a single call. The function handles multi-day event continuation days. Full implementation code is in the plan.
</context>

<next_action>
1. Run: `git checkout gsd/phase-01-build-pipeline`
2. Invoke `superpowers:subagent-driven-development` OR manually dispatch Task 2 implementer subagent with the full Task 2 text from the plan file
3. After implementer: spec review → code quality review → mark Task 2 complete → Task 3, etc.
</next_action>
