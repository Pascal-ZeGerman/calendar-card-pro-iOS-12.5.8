---
context: default
phase: null
task: 3
total_tasks: 6
status: in_progress
last_updated: 2026-06-10T19:15:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Current State

<current_state>
Executing PR #1 bug fixes on branch `gsd/phase-01-build-pipeline`.
Tasks 1 and 2 DONE. Ready to start Task 3.

NOTE: Original plan file (`docs/superpowers/plans/2026-06-09-pr1-bug-fixes.md`) was never committed and is missing.
Task descriptions are reconstructed from HANDOFF context. Read source file directly to understand what still needs fixing.
</current_state>

## Completed Work

<completed_work>
- Task 1: NaN date guards in `eventStart` and `eventEnd` (C3) — DONE (commit `81f12af`)
  - Both functions return `null` + `console.warn` for unparseable datetimes
  - `tests/test-nan-guards.js` created, 6 assertions, all pass

- Task 2: Extract `renderEventTimeText` helper; fix `renderCard` (C2, C4) — DONE
  - `renderEventTimeText(ev, cfg, use24h, isContinuation)` added after `monthShort` (section 3 helpers, ~line 334)
  - renderCard time block: 25 lines (duplicate var decls) → 15 lines (C2 fixed)
  - End time now uses `eventEnd()` instead of `new Date(ev.end.dateTime)` directly (C4 fixed)
  - Multi-day timed events show "Continues" on continuation days (new feature)
  - `tests/test-render-event-time-text.js` created, 7 assertions, all pass
</completed_work>

## Remaining Work

<remaining_work>
- Task 3: Add `_renderMessage`; fix fetch-failure error surface (C1, I1) — **START HERE**
- Task 4: Loading placeholder, expand-state persistence, stale-hass re-render (I2, I3, I4, I6)
- Task 5: Validate entity entries; throw on zero valid entities (I5)
- Task 6: Comment fixes (verify-bundle.sh, helpers.ts, calendar-card-pro.js)
</remaining_work>

## Decisions Made

<decisions_made>
- Scope: all 10 findings (C1-C4 + I1-I6 + comment fixes) — user chose full scope
- Approach B: extract `renderEventTimeText` + `_renderMessage` helpers (cleaner, testable)
- Task 1 spec reviewer advisory gap accepted — test stubs don't call console.warn, but production code does
</decisions_made>

## Infrastructure State

- Branch: `gsd/phase-01-build-pipeline`
- No running servers
- No build step needed for Tasks 3-6 (source-level edits only)
- Node.js test runner: `node tests/test-*.js` (no framework, built-in `assert` only)
- Source file: `src-lite/calendar-card-pro.js` (~863 lines after Tasks 1+2)

## Task 3 Context (C1, I1)

**C1** — PR reviewer finding: fetch-failure path (the `.catch` in `fetchCalendarEvents`) silently swallows errors and returns `[]`. The card shows nothing but gives the user no feedback that a calendar entity failed to load.

**I1** — Improvement: surface the error visually using a `_renderMessage(text)` helper that appends a styled message element inside the container, similar to the "No upcoming events" empty-day rendering but at card level.

The `_renderMessage` approach mirrors the pattern from Task 2: a testable pure-DOM helper called from `_fetchAndRender`'s catch path. The fetch catch in `_fetchAndRender` (around line 770) currently only logs to console.

<next_action>
1. Read `src-lite/calendar-card-pro.js` (focus on `_fetchAndRender`, `fetchCalendarEvents`, and `_render` methods)
2. Add `_renderMessage(text)` instance method to CalendarCardPro (after `_render`, before HA lifecycle methods)
3. Wire fetch failure in `_fetchAndRender` catch to call `this._renderMessage('Calendar data unavailable')`
4. Write `tests/test-render-message.js` (Node.js, built-in assert, DOM stubs)
5. Commit as: `fix: add _renderMessage helper; surface fetch errors in card UI (C1, I1)`
</next_action>
