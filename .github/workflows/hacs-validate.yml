name: HACS Validation

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *' # Run daily at midnight
  workflow_dispatch:

jobs:
  validate-hacs:
    if: github.repository == 'Pascal-ZeGerman/calendar-card-pro-iOS-12.5.8'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: HACS validation
        uses: hacs/action@main
        with:
          category: 'plugin'
